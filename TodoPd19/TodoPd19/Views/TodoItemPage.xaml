<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="TodoPd19.Views.TodoItemPage"
             Title="{Binding Name}">

  <StackLayout Margin="20" VerticalOptions="StartAndExpand">
    <Label Text="Name" />
    <Entry Text="{Binding Name}" />
    <Label Text="Notes" />
    <Entry Text="{Binding Notes}" />
    <Label Text="Done" />
    <Switch IsToggled="{Binding Done, Mode=TwoWay}" />
    <Button Text="Save" Command="OnSaveClicked" />
    <Button Text="Delete" Command="OnDeleteClicked" />
    <Button Text="Cancel" Command="OnCancelClicked" />
    <Button Text="Speak" Command="OnSpeakClicked" />
  </StackLayout>

</ContentPage>